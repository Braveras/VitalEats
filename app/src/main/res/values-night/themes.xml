<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.VitalEats" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/greend</item>
        <item name="colorPrimaryVariant">@color/green2d</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/oranged</item>
        <item name="colorSecondaryVariant">@color/purple2d</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
    </style>

    <style name="Toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/greend</item>
        <item name="android:elevation" tools:targetApi="lollipop">
            @dimen/cardview_compat_inset_shadow
        </item>
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <!--<item name="navigationIcon">?attr/homeAsUpIndicator</item>-->
    </style>

    <style name="Hint" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/green2d</item>
        <item name="colorPrimaryDark">@color/green2d</item>
        <item name="colorAccent">@color/green2d</item>
        <item name="android:textColorHint">@color/green2d</item>
    </style>
</resources>